{% extends 'base.html.twig' %}

{% block title %}RendezVous{% endblock %}

{% block body %}
    <h1>RendezVous</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rendez_vou.id }}</td>
            </tr>
            <tr>
                <th>Lib_rdv</th>
                <td>{{ rendez_vou.libRdv }}</td>
            </tr>
            <tr>
                <th>Date_rdvAt</th>
                <td>{{ rendez_vou.dateRdvAt ? rendez_vou.dateRdvAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_rendez_vous_index') }}">back to list</a>

    <a href="{{ path('app_admin_rendez_vous_edit', {'id': rendez_vou.id}) }}">edit</a>

    {{ include('admin_rendez_vous/_delete_form.html.twig') }}
{% endblock %}
