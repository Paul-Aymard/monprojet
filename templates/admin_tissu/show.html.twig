{% extends 'base.html.twig' %}

{% block title %}Tissu{% endblock %}

{% block body %}
    <h1>Tissu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tissu.id }}</td>
            </tr>
            <tr>
                <th>Libel_tiss</th>
                <td>{{ tissu.libelTiss }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_tissu_index') }}">back to list</a>

    <a href="{{ path('app_admin_tissu_edit', {'id': tissu.id}) }}">edit</a>

    {{ include('admin_tissu/_delete_form.html.twig') }}
{% endblock %}
