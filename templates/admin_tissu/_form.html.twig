{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

<div class="card">
<div class="card-body">
    <h1 class="card-title">Créér un client</h1>

    <!-- Horizontal Form -->
    {{ form_start(form) }}

        <div class="row mb-3">
            <label for="inputEmail3" class="col-sm-2 col-form-label">Libellé tissu</label>
            <div class="col-sm-10">
            {{ form_widget(form.libel_tiss) }}   
            </div>
        </div>
        <div class="row mb-3">
            <label for="inputprenom" class="col-sm-2 col-form-label">Designation du fournisseur</label>
            <div class="col-sm-10">
                {{ form_widget(form.Design_fou) }}
            </div>
        </div>
        <div class="text-center">
            <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
            <button type="reset" class="btn btn-secondary">Reset</button>
        </div>
    {{ form_end(form) }}
    <!-- End Horizontal Form -->
    

</div>
</div> 


