{% extends 'base.html.twig' %}

{% block title %}Employe{% endblock %}

{% block body %}
    <h1>Employe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ employe.id }}</td>
            </tr>
            <tr>
                <th>Nom_emp</th>
                <td>{{ employe.NomEmp }}</td>
            </tr>
            <tr>
                <th>Pre_emp</th>
                <td>{{ employe.PreEmp }}</td>
            </tr>
            <tr>
                <th>Tel_emp</th>
                <td>{{ employe.telEmp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_employe_index') }}">back to list</a>

    <a href="{{ path('app_admin_employe_edit', {'id': employe.id}) }}">edit</a>

    {{ include('admin_employe/_delete_form.html.twig') }}
{% endblock %}
