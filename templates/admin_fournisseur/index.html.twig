{% extends 'base.html.twig' %}

{% block title %}Fournisseur index{% endblock %}

{% block body %}
    <h1>Fournisseur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Design_fou</th>
                <th>Tel_fou</th>
               
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fournisseur in fournisseurs %}
            <tr>
                <td>{{ fournisseur.id }}</td>
                <td>{{ fournisseur.designFou }}</td>
                <td>{{ fournisseur.telFou }}</td>
                {# <td>{{ fournisseur.Noem_emp}}</td> #}
                <td>
                    <a href="{{ path('app_admin_fournisseur_show', {'id': fournisseur.id}) }}">show</a>
                    <a href="{{ path('app_admin_fournisseur_edit', {'id': fournisseur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_fournisseur_new') }}">Create new</a>
{% endblock %}
