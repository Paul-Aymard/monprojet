{% extends 'base.html.twig' %}

{% block title %}Facture{% endblock %}

{% block body %}
    <h1>Facture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ facture.id }}</td>
            </tr>
            <tr>
                <th>Dat_factAt</th>
                <td>{{ facture.datFactAt ? facture.datFactAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Mont_fact</th>
                <td>{{ facture.montFact }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_facture_index') }}">back to list</a>

    <a href="{{ path('app_admin_facture_edit', {'id': facture.id}) }}">edit</a>

    {{ include('admin_facture/_delete_form.html.twig') }}
{% endblock %}
