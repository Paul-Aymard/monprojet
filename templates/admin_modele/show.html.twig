{% extends 'base.html.twig' %}

{% block title %}Modele{% endblock %}

{% block body %}
    <h1>Modele</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ modele.id }}</td>
            </tr>
            <tr>
                <th>Libel_mod</th>
                <td>{{ modele.libelMod }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_modele_index') }}">back to list</a>

    <a href="{{ path('app_admin_modele_edit', {'id': modele.id}) }}">edit</a>

    {{ include('admin_modele/_delete_form.html.twig') }}
{% endblock %}
