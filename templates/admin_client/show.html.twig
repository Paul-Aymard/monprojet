{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>Nom_cli</th>
                <td>{{ client.NomCli }}</td>
            </tr>
            <tr>
                <th>Pre_cli</th>
                <td>{{ client.PreCli }}</td>
            </tr>
            <tr>
                <th>Tel_cli</th>
                <td>{{ client.TelCli }}</td>
            </tr>
            <tr>
                <th>Sex_cli</th>
                <td>{{ client.SexCli }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_client_index') }}">back to list</a>

    <a href="{{ path('app_admin_client_edit', {'id': client.id}) }}">edit</a>

    {{ include('admin_client/_delete_form.html.twig') }}
{% endblock %}
